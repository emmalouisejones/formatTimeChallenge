Function to process an integer representing a time and return a string in a more readable format.

Steps to run locally :

clone this repo
install jest : npm install --save-dev jest
run tests : npm run test 